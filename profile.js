async function initProfile(){const u=await requireAuth();if(!u)return;const {data:p}=await supabaseClient.from("profiles").select("name,full_name,email,avatar_url,credits").eq("id",u.id).maybeSingle();const name=p?.full_name||p?.name||u.user_metadata?.full_name||u.email?.split("@")[0]||"InkSpire";document.getElementById("profile-name").textContent=name;document.getElementById("profile-email").textContent=p?.email||u.email||"—";document.getElementById("profile-credits").textContent=Math.max(0,Number(p?.credits??0))+" / 50";if(p?.avatar_url)document.getElementById("profile-avatar").innerHTML='<img src="'+p.avatar_url+'" alt="">'}initProfile();